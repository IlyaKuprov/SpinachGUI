gTrackBar is now built as a standard VB.NET class library project.

Build it through the solution or directly with MSBuild/Visual Studio:

  msbuild gTrackBar.vbproj /p:Configuration=Release

The main SpinachGUI projects reference gTrackBar as a normal managed project/assembly.
No .netmodule build or #using directive is required anymore.
